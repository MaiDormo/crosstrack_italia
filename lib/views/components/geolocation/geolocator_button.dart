import 'package:flutter/material.dart';

class GeolocatorButton extends StatelessWidget {
  const GeolocatorButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
